<UserControl
    x:Class="Bingo.UserControl.BingoNumber"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Bingo.UserControl"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="80"
    d:DesignWidth="80">

    <Border Name="brdrDate" Background="#66000000"  DataContext="{Binding}"  BorderThickness="1" Margin="10" BorderBrush="#ececec">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <!--<RowDefinition/>-->
            </Grid.RowDefinitions>
            <TextBlock  Text="{Binding NumberString}" Foreground="{Binding BackColor}" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center" Style="{StaticResource BingoNumberStyle}" />

        </Grid>
    </Border>
</UserControl>
